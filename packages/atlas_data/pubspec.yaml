name: atlas_data
description: Sovereign Atlas Engine normalized feature model and datasets.
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'
# No dependencies (ADR-002). Engine edges: core, geo, provider_api.
