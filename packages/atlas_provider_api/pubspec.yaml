name: atlas_provider_api
description: Sovereign Atlas Engine provider contracts only (no implementations).
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'
# No dependencies (ADR-002). Engine edges: core.
