name: atlas_providers
description: Sovereign Atlas Engine concrete provider implementations (ADR-003).
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  atlas_core:
    path: ../atlas_core
  atlas_provider_api:
    path: ../atlas_provider_api
  atlas_tiles:
    path: ../atlas_tiles
