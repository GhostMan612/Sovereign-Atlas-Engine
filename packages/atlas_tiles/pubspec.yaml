name: atlas_tiles
description: Sovereign Atlas Engine tile addressing, cache, pipeline, execution.
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'
# No dependencies (ADR-002). Engine edges: core, geo, provider_api.
