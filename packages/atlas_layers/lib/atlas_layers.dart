// Sovereign Atlas Engine — atlas_layers public barrel.
//
// Phase 0.5 vertical slice. Relative imports keep the slice verifiable with a
// bare Dart SDK and no package manifests (DEC-016 open). At workspace
// initialization these become `package:atlas_layers/...` imports.

export 'src/definition/layer_definition.dart';
