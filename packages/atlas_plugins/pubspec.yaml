name: atlas_plugins
description: Sovereign Atlas Engine plugin manifest, registry and permissions.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  atlas_core:
    path: ../atlas_core
