# Integrations

Adapters for external applications such as Sovereign Mantle and Recovery for All. Integration code must depend on the app's abstractions (pure-logic modules, provider interfaces) rather than forcing the host to depend on external applications.
